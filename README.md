
  # Create landing page with animations

  This is a code bundle for Create landing page with animations. The original project is available at https://www.figma.com/design/ssHzh0GZeUQOOV12mLJ2XR/Create-landing-page-with-animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  